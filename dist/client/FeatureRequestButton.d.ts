import { type ReportButtonProps } from "./ReportWidget";
/** Floating "Request feature" button — lighter capture (no console errors). */
export declare function FeatureRequestButton(props: ReportButtonProps): import("react").JSX.Element;
