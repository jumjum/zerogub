import { type ReportButtonProps } from "./ReportWidget";
/** Bug + feature buttons side by side in one floating bar. */
export declare function ZeroGBar({ projectKey, collectorUrl, reporter, placement, enabled, }: ReportButtonProps): import("react").JSX.Element | null;
